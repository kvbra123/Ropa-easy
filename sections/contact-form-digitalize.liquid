
<section id="contact-digitalize" class="py-32 bg-white">
  <div class="max-w-[1400px] mx-auto px-6 lg:px-12">
    <div class="grid grid-cols-1 lg:grid-cols-12 gap-20">
      
      <div class="lg:col-span-5">
        <span class="text-[10px] font-black uppercase tracking-[0.4em] text-orange-500 mb-6 block">B2B Solutions</span>
        <h2 class="text-6xl font-display italic text-slate-900 leading-tight mb-8">Evoluciona tu <br>Catálogo.</h2>
        <p class="text-[11px] text-slate-400 uppercase tracking-widest leading-loose max-w-sm">
          Transformamos inventarios físicos en activos 1:1 optimizados para la venta digital. Completa el registro técnico para comenzar.
        </p>
      </div>

      <div class="lg:col-span-7 bg-slate-50 p-12 lg:p-20 border border-slate-100">
        {% form 'contact' %}
          {% if form.posted_successfully? %}
            <div class="text-center py-12 animate-in zoom-in">
              <h3 class="text-3xl font-display italic mb-4">Recibido.</h3>
              <p class="text-[10px] uppercase tracking-widest text-slate-400">Nuestro equipo técnico te contactará en 24h.</p>
            </div>
          {% else %}
            <div class="grid grid-cols-1 md:grid-cols-2 gap-12 mb-12">
              <div class="border-b border-slate-200 focus-within:border-slate-900 transition-all">
                <label class="text-[9px] font-black uppercase tracking-widest text-slate-400">Marca / Empresa</label>
                <input required type="text" name="contact[brand]" class="w-full py-4 bg-transparent outline-none text-xs font-bold uppercase" placeholder="Nombre Comercial">
              </div>
              <div class="border-b border-slate-200 focus-within:border-slate-900 transition-all">
                <label class="text-[9px] font-black uppercase tracking-widest text-slate-400">Email Corporativo</label>
                <input required type="email" name="contact[email]" class="w-full py-4 bg-transparent outline-none text-xs font-bold uppercase" placeholder="hola@marca.com">
              </div>
            </div>
            <div class="border-b border-slate-200 focus-within:border-slate-900 transition-all mb-16">
              <label class="text-[9px] font-black uppercase tracking-widest text-slate-400">Volumen del Catálogo</label>
              <select name="contact[size]" class="w-full py-4 bg-transparent outline-none text-xs font-bold uppercase appearance-none">
                <option>1-10 Prendas</option>
                <option>11-50 Prendas</option>
                <option>50+ Prendas</option>
              </select>
            </div>
            <button type="submit" class="w-full py-6 bg-slate-900 text-white text-[10px] font-black uppercase tracking-[0.4em] hover:bg-orange-500 transition-all shadow-xl">
              Enviar Solicitud Técnica
            </button>
          {% endif %}
        {% endform %}
      </div>

    </div>
  </div>
</section>
