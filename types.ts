
export interface FormData {
  name: string;
  email: string;
  regionCode: string;
  phone: string;
  brandName: string;
  catalogSize: string;
  message: string;
}

export type ViewState = 'landing' | 'digitalization-form' | 'image-editor';
