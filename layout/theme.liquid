
<!doctype html>
<html class="no-js" lang="{{ request.locale.iso_code }}">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>Ropa Easy | Digitalización & IA</title>
    
    {{ content_for_header }}

    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;800&family=Playfair+Display:ital,wght@0,700;1,700&display=swap" rel="stylesheet">
    
    <style>
      :root {
        --brand-orange: #f0945c;
        --brand-blue: #89c4e1;
      }
      body { 
        font-family: 'Inter', sans-serif; 
        -webkit-font-smoothing: antialiased;
        background: white;
      }
      .font-display { font-family: 'Playfair Display', serif; }
      .editorial-grid { border-left: 1px solid #f1f5f9; border-right: 1px solid #f1f5f9; }
      
      /* Aseguramos que el root ocupe el espacio necesario */
      #root { min-height: 100vh; }
    </style>
  </head>

  <body class="bg-white text-slate-900 antialiased overflow-x-hidden">
    <!-- Solo renderizamos el root de React para evitar duplicados con Liquid -->
    <div id="root"></div>

    <main id="MainContent" class="hidden">
      {{ content_for_layout }}
    </main>
  </body>
</html>
